---
description: Show carry-on state — pending wakes and recent resume history
---

Run `"${CLAUDE_PLUGIN_ROOT}/bin/carry-on" status` with the Bash tool and show
the user its output verbatim in a code block. Do not paraphrase the table.
